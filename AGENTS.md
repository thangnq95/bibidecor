# Repo Instructions

- When starting the local dev server or ngrok for this repository, always use port `4173`.
- Do not change the port to any other value unless the user explicitly asks for it.
- Treat requests like `Start ngrok`, `Run dev`, or `Start ngrok và run dev` as `4173` by default.
